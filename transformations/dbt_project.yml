name: 'olist_transformations'
version: '1.0.0'
config-version: 2

profile: 'olist_transformations'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_transformations:
    example:
      +materialized: view
